## 1. Design and implementation
- [x] 1.1 Resolve this stage's implementation details and dependency review.
- [x] 1.2 Implement the selected common Kotlin parser and matcher as a production module, preserving validated record semantics and adding the production token index, metadata, and full Unicode characterization. Move the five publisher bundles and manifest to one reviewed resource source, with legacy indicator cache handling.

## 2. Acceptance
- [x] 2.1 Canonical vectors, publisher-feed metadata/deduplication, large synthetic workloads, live cancellation and both native binding suites pass.
- [x] 2.2 Record validation and update affected specs before archiving.
