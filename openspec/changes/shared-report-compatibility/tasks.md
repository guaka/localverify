## 1. Design and implementation
- [ ] 1.1 Resolve this stage's implementation details and dependency review.
- [ ] 1.2 Add a shared report model, version-1 ISO-8601 export serializer, escaped HTML and ZIP exports. Keep explicit adapters for existing Swift and Android saved-case encodings, optional metadata and opt-in originals.

## 2. Acceptance
- [ ] 2.1 Synthetic historical cases from both serializers reopen without lost findings or invented metadata; export content, hashes, failure cleanup and original opt-in pass.
- [ ] 2.2 Record validation and update affected specs before archiving.
